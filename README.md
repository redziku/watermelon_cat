# watermelon_cat

UI 설계서 PPTX에서 슬라이드별 UI 정보를 뽑아 CSV / Excel로 바꿔 주는 추출기입니다.

## 설치

```bash
pip install -r requirements.txt
```

## 사용법

```bash
# 파일 하나
python ui_spec_extract.py 설계서.pptx

# 폴더 안의 pptx 전부 (결과는 한 벌로 합쳐짐)
python ui_spec_extract.py ./inbox -o ./out

# 하위 폴더까지
python ui_spec_extract.py ./inbox -r -o ./out
```

`-o` 를 생략하면 현재 폴더 아래 `out/` 에 저장합니다.
몇 개를 넣든 산출물은 항상 한 벌이며, 파일 하나가 손상돼 있어도 그 파일만
건너뛰고 나머지는 처리합니다.

**산출물을 엑셀에서 열어 둔 채로 실행하면 저장이 안 됩니다.** 실행을
시작하기 전에 검사해 바로 알려 주므로, 엑셀을 닫고 다시 실행하시면 됩니다.

### 윈도우 / 구글드라이브 경로

경로에 공백이나 한글이 있으면 큰따옴표로 감쌉니다.

```powershell
python ui_spec_extract.py "G:\내 드라이브\차세대 정보계 첨부\00. 메일수신\신용실적_올랩" -o ".\out"
```

구글 드라이브 데스크톱이 **스트리밍 모드**면 파일 실체가 아직 PC에 없어
첫 실행이 느리거나 오프라인에서 실패할 수 있습니다. 해당 폴더를 우클릭해
`오프라인 액세스 사용` 으로 바꿔 두면 안정적입니다.

## 산출물

| 파일 | 형태 | 용도 |
|---|---|---|
| `ui_list.csv` | UI 1개 = 1행 | 화면 목록, 진행 현황 관리 |
| `ui_flow.csv` | 흐름 항목 1개 = 1행 | 요구사항 추적, 테스트케이스 매핑 |
| `ui_element.csv` | 화면 구성요소 1개 = 1행 | 버튼·조회조건·데이터 항목 인벤토리 |
| `doc_list.csv` | 문서(PPTX) 1개 = 1행 | 문서번호·버전·작성일 관리 |
| `ui_spec.xlsx` | 위 셋 + 버튼 집계를 시트로 | 바로 열어 보는 용도 (필터·틀고정 적용됨) |

엑셀 시트는 `문서` / `UI목록` / `화면별버튼` / `화면요소` / `UI흐름_상세` /
`버튼인벤토리` / `중복점검` 일곱 장이며, 모두 엑셀 **표**로 지정돼 있어 피벗테이블을 만들 때
범위가 자동으로 잡힙니다.

## 어떤 시트를 보면 되나

- **화면끼리 버튼 구성을 비교하고 싶을 때 → `화면별버튼`**
  행이 화면, 열이 버튼인 매트릭스입니다. 버튼이 있으면 `●` 가 찍힙니다.
  열은 많이 쓰이는 버튼부터 왼쪽에 놓여 있어, **왼쪽 덩어리에 생긴 구멍이
  곧 공통 버튼이 빠진 화면**입니다. 고정 컬럼은 틀고정돼 있어 오른쪽으로
  스크롤해도 화면 이름이 따라옵니다.
  보고서 유형은 버튼 영역이 없어 한 줄이 통째로 비는 것이 정상입니다.
- **화면 하나를 빠르게 훑고 싶을 때 → `UI목록`**
  한 행에 `조회조건` / `버튼` / `데이터` 가 각각 한 칸으로 정리돼 있고,
  `버튼수` 로 정렬하면 버튼이 많은 화면부터 볼 수 있습니다.
- **피벗이나 별도 데이터표를 만들고 싶을 때 → `화면요소` 또는 `UI흐름_상세`**
  두 시트는 `대분류`·`중분류`·`UI유형` 같은 분류 컬럼을 이미 품고 있어
  **시트 하나만 선택하면 바로 피벗이 됩니다.** 다른 시트와 연결할 필요가 없습니다.

## 시트 구조

시트를 하나로 합치지 않고 `UI_KEY` 로 잇습니다.

```
문서 ──< UI목록 ──< UI흐름_상세
    (문서키)  │ (UI_KEY)
              └─< 화면요소 ──> 버튼인벤토리
```

- `UI_KEY` 는 `NHNIS-BC-DS03-v0.7-s4` 형태로, 문서번호 + 버전 + 슬라이드번호입니다
- 파일명이 아니라 문서번호를 쓰므로 `... (1).pptx` 처럼 이름이 바뀌어도 키는 그대로입니다
- 같은 문서번호·버전이 두 번 들어오면 뒤에 `#2` 가 붙어 키가 겹치지 않습니다
- 같은 `UI_ID` 가 여러 문서에 있으면 `중복점검` 시트에 모입니다 (자동 삭제하지 않습니다)

엑셀에서 두 시트를 붙여 보려면 `UI_KEY` 로 `VLOOKUP` 하거나 피벗테이블의
관계 기능을 쓰면 됩니다.

### `ui_list.csv` 컬럼

`파일명`, `슬라이드`, `대분류`, `중분류`, `소분류`, `UI명`, `UI_ID`, `UI유형`,
`UI_KEY`, `파일명`, `문서번호`, `버전`, `슬라이드`, `슬라이드_전체`, `분할쪽수`,
`대분류`, `중분류`, `소분류`,
`UI명`, `UI_ID`, `UI유형`, `UI유형_원문`, `UI설명`,
`조회조건`, `입력`, `버튼`, `데이터`, `기타영역`,
`조회조건수`, `입력수`, `버튼수`, `데이터수`,
`업무처리흐름`, `기타사항`

`UI유형` 은 원본의 `■화면  □팝업  □보고서` 에서 체크된 것만 골라
`화면` / `팝업` / `보고서` 로 정리한 값입니다.

### `ui_flow.csv` 컬럼

분류 컬럼 8개(`UI_KEY`, `문서번호`, `버전`, `슬라이드`, `대분류`, `중분류`,
`소분류`, `UI_ID`, `UI명`, `UI유형`) + `섹션`, `순번`, `내용`

`UI 흐름 및 기타사항` 칸의 항목을 한 줄에 하나씩 펼친 것입니다.

### `ui_element.csv` 컬럼

위와 같은 분류 컬럼 + `영역`, `영역구분`, `순번`, `요소명`

`화면 정의` 의 `조회조건 영역 : 기준일, 사무소, 단위` 같은 한 줄을 요소 단위로
쪼갠 것입니다. `영역` 은 원본 표기 그대로이고, `영역구분` 은 이를
`버튼` / `조회조건` / `입력` / `데이터` / `기타` 로 분류한 값입니다.
`입력` 은 `등록 영역`·`입력 영역` 처럼 저장할 값을 넣는 곳으로, 걸러 보는
`조회조건` 과 구분합니다.
`영역구분` 이 `버튼` 인 행만 필터링하면 화면별 버튼 목록이 됩니다.

`전일(실적, 대비)` 처럼 괄호 안에 콤마가 있어도 쪼개지지 않습니다.

## 여러 장에 걸친 화면

한 화면을 `이름(1/2)`, `이름(2/2)` 처럼 두 장에 나눠 그린 경우, **한 행으로
합쳐서** 냅니다. UI명에서 쪽 표기는 떼어내고, `슬라이드_전체` 에 `4, 5` 처럼
출처를 남기며 `분할쪽수` 에 몇 장이었는지 기록합니다.

`UI흐름_상세` 와 `화면요소` 의 각 행은 원래 슬라이드 번호를 유지하므로
어떤 항목이 몇 번째 장에서 왔는지 되짚을 수 있습니다.

합치는 대상은 쪽 표기가 있고 `UI_ID` 와 이름이 같은 것뿐이라, 표기 없는
화면이 휩쓸려 합쳐지지 않습니다.

## 인식 대상 슬라이드

`UI 명` / `UI ID` / `UI 유형` 라벨을 가진 4행 6열 표가 있는 슬라이드만 처리하며,
표지·목차·EOD 슬라이드는 자동으로 건너뜁니다.

## 템플릿이 바뀌면

`ui_spec_extract.py` 상단의 `템플릿 매핑` 블록에서 도형 이름과 셀 위치만
고치면 됩니다. 라벨이 예상과 다르면 실행 중 `[경고]` 를 출력하므로,
조용히 엉뚱한 값이 들어가는 일은 없습니다.

자세한 구조 분석과 설계 결정은 `context-notes.md` 를 참고하세요.
