# UI 설계서 PPTX → CSV/Excel 추출 체크리스트

## 1. 구조 분석
- [x] 샘플 PPTX 도형 구조 덤프 (probe)
- [x] 대상 슬라이드 판별 조건 확정 (레이아웃 `본문` + 4x6 표)
- [x] 필드별 소스 위치 확정 (플레이스홀더 이름 + 표 셀 좌표)
- [x] `UI 흐름 및 기타사항` 문단 레벨 구조 확인

## 2. 구현
- [x] 추출기 `ui_spec_extract.py` 작성
- [x] 파일 1개 / 폴더 일괄 처리 지원
- [x] `UI 유형` 체크박스(■/□) 정규화
- [x] `UI 흐름` 을 3개 섹션으로 분해
- [x] 와이드 시트(UI 1개 = 1행) 출력
- [x] 롱 시트(항목 1개 = 1행) 출력
- [x] CSV(UTF-8 BOM) + XLSX 동시 출력

## 3. 검증
- [x] 샘플 파일로 실행, 슬라이드 4~7 추출 확인
- [x] 표지/목차/EOD 슬라이드 제외 확인
- [x] CSV 한글 깨짐 여부 확인
- [x] XLSX 2개 시트 생성 확인

## 3-b. 화면 구성요소 분해 (추가)
- [x] `화면 정의` 항목을 `영역명 : 요소목록` 으로 분리
- [x] 괄호 안 콤마 보호 파서 작성
- [x] 영역 이름 키워드 분류 (버튼 / 조회조건 / 데이터 / 기타)
- [x] `ui_element.csv` + `화면요소` 시트 출력
- [x] `버튼인벤토리` 시트 (버튼명별 사용 화면 집계)
- [x] 보고서 슬라이드의 비표준 영역명 분류 확인

## 3-c. 다중 파일 데이터베이스화 (추가)
- [x] 표지 슬라이드에서 문서 메타 추출 (문서번호/문서명/버전/작성일)
- [x] `UI_KEY` 설계 및 전 시트 부여
- [x] 문서키 충돌 시 자동 구분 (`#2`)
- [x] `문서` 시트 추가
- [x] `중복점검` 시트 추가 (같은 UI_ID 가 여러 문서에 존재)
- [x] 하위 폴더 탐색 옵션 `-r`
- [x] 파일 1개가 깨져도 나머지 계속 처리
- [x] 동일 문서 2개로 키 유일성 검증

## 3-d. 엑셀 사용성 (추가)
- [x] `UI목록` 의 화면정의를 조회조건/버튼/데이터 컬럼으로 분리
- [x] 영역별 개수 컬럼 추가 (조회조건수/버튼수/데이터수)
- [x] 롱 시트에 분류 컬럼 동봉 (단독 피벗 가능)
- [x] 전 시트를 엑셀 '표'(ListObject)로 지정
- [x] 피벗 소스 시트는 줄바꿈 해제
- [x] 시트 순서를 사용 빈도순으로 정렬

## 3-e. 진단 가능성 (추가)
- [x] 파일 처리 전에 진행 상황 출력 (멈춤 / 느림 구분)
- [x] 파일 크기 표시 (드라이브 스트리밍 지연 판단용)
- [x] 실패 시 전체 트레이스백 출력
- [x] 윈도우 cp949 콘솔에서 출력이 죽지 않도록 처리
- [x] 마지막에 실패 파일 목록 요약
- [x] 손상 파일로 동작 검증

## 3-f. 실데이터 적용 후 수정 (추가)
- [x] 파워포인트 줄바꿈(\x0b) 때문에 엑셀 저장이 실패하던 문제 수정
- [x] 저장 직전 제어문자 2차 방어
- [x] 문서명에서 편 이름을 뽑아 문서키에 반영
- [x] 실제 3개 파일(179 UI / 3721 요소)로 검증
- [x] 중첩 괄호 요소 분리 확인

## 3-g. 사용 요청 반영 (추가)
- [x] `화면별버튼` 시트 추가 (화면 1개 = 1행, 버튼만)
- [x] `화면별버튼` 을 화면 × 버튼 매트릭스로 개선
- [x] 버튼 컬럼을 사용 화면수 내림차순 정렬
- [x] 헤더 세로쓰기 + 표시 셀 배경색 + 틀고정
- [x] 분류 못 한 영역 이름을 실행 끝에 건수와 함께 출력
- [x] 미분류 영역 이름 확인 후 분류 규칙 보강
- [x] `입력` 영역구분 신설 (등록/입력 영역)
- [x] 코드·카테고리 영역을 데이터로 분류
- [x] 콜론 없는 줄을 앞 영역의 연속으로 처리
- [x] `화면리스트 영역` 등 목록성 영역 규칙 추가
- [x] 분류 규칙 우선순위 재정렬 (결과·목록이 조회보다 먼저)
- [x] 기존 13종 회귀 검증 통과
- [x] 실행 로그에 버전 출력
- [ ] 원본 오타 여부 확인 (추출 과정 변형 아님을 검증 완료)

## 3-h. 분할 화면 병합 (추가)
- [x] `(1/2)(2/2)` 쪽 표기 파서
- [x] UI명에서 쪽 표기 분리
- [x] 같은 문서·UI_ID·이름끼리만 병합
- [x] 흐름·요소 중복 제거 후 순번 재부여
- [x] 병합 후 영역 컬럼 재계산
- [x] 쪽 표기와 실제 슬라이드 수가 다르면 경고
- [x] 실데이터 7쌍 병합 검증 (179 → 172)

## 3-i. 저장 실패 방어 (추가)
- [x] 실행 시작 전 산출물 잠금 여부 검사
- [x] 잠겨 있으면 파일을 읽기 전에 중단
- [x] 실행 도중 잠긴 경우 마지막 방어
- [x] 잠김 재현 테스트

## 3-j. 읽기안내 시트 (추가)
- [x] 역할별 길잡이 표
- [x] 시트명에 내부 하이퍼링크
- [x] 실행 시점 건수·버전 자동 기재
- [x] 맨 앞 배치 + 열었을 때 선택되도록
- [x] 링크 대상 존재 여부 검증

## 4. 마무리
- [x] README 사용법 작성
- [x] requirements.txt 작성
- [x] 커밋 및 푸시
