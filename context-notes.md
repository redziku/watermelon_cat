# 컨텍스트 노트 — 결정과 이유

## 샘플 파일 구조 (NHNIS-BC-DS03 UI 설계서 v0.7)

슬라이드 크기 29.7 x 21.0 cm (A4 가로), 총 8장.

| 슬라이드 | 레이아웃 | 성격 |
|---|---|---|
| 1 | 표지 | 문서 메타(문서번호, 버전, 일자) |
| 2 | 목차 | 개정이력 표 |
| 3 | 목차 | 목차 |
| 4~7 | 본문 | **UI 설계 본문 (추출 대상)** |
| 8 | EOD | 빈 종료 페이지 |

## 필드 소스 위치

### 슬라이드 상단 (플레이스홀더, 이름으로 식별)
- `제목 1` → 소분류. 예) `2.1.1 종합현황`
- `텍스트 개체 틀 3` → 대분류. 예) `2. 종합실적`
- `텍스트 개체 틀 4` → 중분류. 예) `2.1 종합현황`

### 본문 표 (`Group 56`, 4행 6열)
```
r0: [UI 명 | 값 | UI ID | 값 | UI 유형 | 값]
r1: [UI 설명 | 값(1x5 병합) ]
r2: [UI 레이아웃(1x4 병합) | UI 흐름 및 기타사항(1x2 병합)]
r3: [ (레이아웃 이미지 영역, 텍스트 없음) | UI 흐름 본문(1x2 병합) ]
```

## 핵심 결정

**결정 1 — 좌표가 아니라 표 셀 좌표로 식별한다.**
처음엔 도형 좌표 기반을 예상했으나, 실제 파일은 모든 핵심 데이터가 단 하나의
4x6 표 안에 들어 있었다. 표 셀은 좌표보다 훨씬 안정적이라(도형을 조금 옮겨도
셀 주소는 안 변함) 좌표 매칭 로직 자체가 불필요해졌다.

**결정 2 — 라벨 검증을 넣는다.**
`r0c0 == 'UI 명'` 처럼 라벨 셀 값을 확인한 뒤에만 값을 읽는다. 템플릿이 바뀌면
조용히 잘못된 값을 뽑는 대신 경고를 내도록 하기 위함이다.

**결정 3 — `UI 유형`은 체크박스를 정규화한다.**
원본은 `■화면  □팝업  □보고서` 형태. ■ 표시된 것만 골라 `화면` / `팝업` /
`보고서` 로 변환한다. 원문도 별도 컬럼으로 함께 보존한다.

**결정 4 — `UI 흐름`은 문단 레벨로 3개 섹션으로 쪼갠다.**
셀 텍스트를 그대로 뽑으면 PowerPoint 자동 번호(1. 2. 3.)가 사라져 한 덩어리가
된다. 문단의 `level` 값을 쓰면 레벨0이 섹션 제목, 레벨1이 항목임을 알 수 있다.
관측된 섹션 제목은 `화면 정의` / `화면의 업무처리 흐름 정의` /
`기타 제약조건 및 특이사항` 3종이며, 레벨0 문단이 나올 때마다 새 섹션으로
전환하므로 제목 문구가 바뀌어도 동작한다.

**결정 5 — 와이드 + 롱 두 벌로 출력한다.**
- 와이드(`_ui_list`): UI 1개 = 1행. 목록/현황 파악용
- 롱(`_ui_flow`): 흐름 항목 1개 = 1행. 요구사항 추적, 테스트케이스 매핑용

엑셀에서 다루기 편한 쪽이 상황마다 다르므로 둘 다 낸다. XLSX는 두 시트를
한 파일에 담는다.

**결정 6 — CSV는 UTF-8 BOM으로 쓴다.**
BOM이 없으면 한국어 윈도우 엑셀에서 더블클릭 시 한글이 깨진다.

**결정 7 — 폴더 감시(watchdog)는 넣지 않았다.**
폴더 인자를 주면 그 안의 `.pptx` 를 전부 처리하므로, 실사용에는 "스크립트
실행" 한 번으로 충분하다. 상시 감시 데몬은 요청이 있을 때 추가한다.

**결정 8 — `화면 정의` 를 영역 / 요소 단위까지 쪼갠다.**
`조회조건 영역 : 기준일, 사무소, 단위` 같은 한 줄을 `영역명` 과 `요소` 로
분해해 `ui_element.csv` 로 낸다. 버튼만 걸러내면 그대로 버튼 인벤토리가 된다.

여기서 두 가지 함정이 있었다.

*함정 1 — 괄호 안 콤마.* `전일(실적, 대비)` 를 단순 `split(",")` 하면
`전일(실적` / `대비)` 로 깨진다. 괄호 깊이를 세면서 깊이 0인 콤마에서만
자르도록 `split_items()` 를 만들었다. 전각 괄호 `（）` 도 함께 처리한다.

*함정 2 — 영역 이름이 고정이 아니다.* 샘플에서만 `조회조건 영역`,
`버튼 영역`, `데이터 영역`, `출력 조건 영역`, `보고서결과 영역` 5종이
나왔다. 하드코딩 대신 콜론 앞을 영역명으로 통째로 보존하고, 별도의
`영역구분` 컬럼에 키워드 규칙으로 `버튼` / `조회조건` / `데이터` / `기타`
를 채운다. 규칙은 위에서부터 먼저 맞는 것을 쓰므로 `버튼` 을 가장 먼저
검사해야 한다(`조회` 와 겹칠 수 있음).

원본 영역명을 지우지 않고 남긴 이유는, 문서마다 다른 명칭 자체가 정보이고
분류 규칙이 틀렸을 때 확인할 근거가 필요하기 때문이다.

**결정 9 — 버튼 인벤토리는 버튼명 기준으로 뒤집어 집계한다.**
`화면요소` 시트에서 버튼만 필터링하면 "화면별 버튼 목록" 은 바로 나온다.
손으로 만들기 어려운 쪽은 반대 방향(버튼 → 이 버튼을 쓰는 화면들)이라
`버튼인벤토리` 시트를 따로 둔다. 사용 화면 수 내림차순으로 정렬해
공통 버튼과 화면 전용 버튼이 위아래로 갈리게 했다.

**결정 10 — 시트를 합치지 않고 `UI_KEY` 로 잇는다(별 구조).**
여러 PPTX 를 한 데이터베이스로 모을 때 목록과 흐름을 한 시트로 합치면
UI 메타(UI명, 설명, 유형 …)가 흐름 항목 수만큼 반복 복제된다. 설계서
하나만 고쳐도 수십 행을 손봐야 하고, "화면이 몇 개인가" 를 세면 흐름 항목
수가 나온다. 그래서 `문서` → `UI목록` → (`UI흐름_상세`, `화면요소`) 의
별 구조를 유지하고 `UI_KEY` 하나로 잇는다.

**결정 11 — 키는 문서번호+버전+슬라이드로 만든다.**
`NHNIS-BC-DS03-v0.7-s4` 형태다. 파일명을 키로 쓰지 않은 이유는 메일로
받은 파일명이 `... (1).pptx` 처럼 쉽게 변하기 때문이다. 사람이 읽고
어느 문서의 몇 번째 장인지 바로 알 수 있는 것도 이점이다.

**결정 12 — 키 충돌은 경고가 아니라 해결한다.**
같은 문서번호·버전이 두 번 들어오면(같은 파일을 중복 수신한 경우)
뒤에 `#2` 를 붙여 구분한다. 처음에는 경고만 냈는데, 키가 겹치면 시트 간
조인이 조용히 어긋나 데이터가 틀린 줄도 모르게 된다. 경고는 그대로 내되
키는 반드시 유일하게 만든다.

**결정 13 — 같은 UI_ID 의 중복은 지우지 않고 표시만 한다.**
`중복점검` 시트에 어느 문서·슬라이드에 있는지 모아 보여 준다. 개정판이
겹친 것인지, 서로 다른 화면에 같은 ID 를 잘못 부여한 것인지는 사람이
판단할 일이라 자동 삭제하지 않는다.

**결정 14 — 한 파일이 깨져도 전체를 멈추지 않는다.**
수십 개를 돌리는 도중 파일 하나가 손상돼 있으면 그 파일만 `[오류]` 로
알리고 건너뛴다. 40개 중 39개를 뽑아 놓고 마지막에 멈추는 것이 최악이다.

**결정 15 — 정규화(별 구조)와 피벗 편의를 둘 다 가져간다.**
별 구조는 관리에 좋지만, 엑셀 피벗테이블은 **평평한 표 하나**를 소스로
삼는다. 시트가 나뉘어 있으면 데이터 모델·관계 기능을 써야 해서 진입장벽이
생긴다. 그래서 롱 시트(`화면요소`, `UI흐름_상세`)에 분류 컬럼
(`문서번호`, `버전`, `대분류`, `중분류`, `소분류`, `UI_ID`, `UI명`, `UI유형`)
을 함께 싣는다. 값이 행마다 반복되지만, 이 시트들은 읽기 전용 파생물이라
갱신 이상이 생기지 않는다. 덕분에 시트 하나만 선택해도 바로 피벗이 된다.

**결정 16 — `UI목록` 의 `화면정의` 원문 컬럼을 없앴다.**
`조회조건 영역 : … / 버튼 영역 : … / 데이터 영역 : …` 라는 한 덩어리는
`조회조건` / `버튼` / `데이터` 컬럼과 내용이 같다. 같은 값을 두 번 두면
어느 쪽이 정본인지 흐려지므로 원문 컬럼을 빼고 분리된 컬럼만 남겼다.
분류 규칙이 못 잡은 영역은 사라지지 않고 `기타영역` 으로 모인다.

**결정 17 — 개수 컬럼을 미리 계산해 둔다.**
`조회조건수` / `버튼수` / `데이터수` 는 피벗으로도 구할 수 있지만, 컬럼으로
있으면 정렬 한 번에 "버튼이 제일 많은 화면" 이 바로 나온다. 빠르게 훑는
것이 이 파일의 첫 번째 목적이라 계산해 둔다.

**결정 18 — 전 시트를 엑셀 '표'(ListObject)로 만든다.**
자동 필터만 걸면 피벗테이블을 만들 때 범위를 매번 지정해야 하고, 행이
늘면 범위가 어긋난다. 표로 지정하면 범위가 자동으로 따라온다.

**결정 19 — 진행 상황을 파일 처리 "전" 에 찍는다.**
원래는 처리가 끝난 뒤 결과와 함께 파일명을 찍었다. 그러면 오래 걸리는
파일에서 화면이 멈춘 것처럼 보여, 정지한 것인지 느린 것인지 구분이 안 된다.
구글드라이브 스트리밍 폴더는 첫 읽기에서 실제로 내려받느라 수십 초가
걸릴 수 있어 더 헷갈린다. 그래서 `[3/12] 파일명 (4.3MB) 처리 중 ...` 을
먼저 flush 해서 찍고, 끝나면 같은 줄에 결과를 잇는다.

**결정 20 — 실패는 요약하지 말고 트레이스백을 전부 내놓는다.**
`except Exception as exc` 에서 `{exc}` 만 찍으면 메시지가 비어 있는 예외도
있어 원인을 못 찾는다. 파일 하나가 실패해도 전체는 계속 돌아가므로,
트레이스백을 길게 찍는 비용보다 원인을 아는 이득이 크다.

**결정 21 — 윈도우 콘솔 인코딩 방어.**
한국어 윈도우 기본 콘솔은 cp949 라, 표현 못 하는 문자가 파일명이나 표
내용에 있으면 `UnicodeEncodeError` 로 죽는다. 진단 메시지를 찍다가
프로그램이 죽는 것은 최악이라 `errors="replace"` 로 바꿔 둔다.

**결정 22 — 파워포인트 줄바꿈(\x0b)을 반드시 변환한다.**
실제 12개 파일을 돌렸을 때 마지막 저장 단계에서
`openpyxl.utils.exceptions.IllegalCharacterError` 로 죽었다. 원인은
파워포인트에서 Shift+Enter 로 넣은 줄바꿈이 `\x0b`(수직 탭)로 들어오는데
엑셀이 이 문자를 셀에 허용하지 않기 때문이다. 업로드받은 `여신실적(2)`
파일에서 16개가 확인됐다.

`\x0b` 와 `\x0c` 는 줄바꿈(`\n`)으로 바꾸고 나머지 제어문자는 버린다.
읽는 쪽(`cell_text`, `find_shape_text`, `parse_flow`, 표지)에서 정리하되,
`add_sheet` 저장 직전에도 한 번 더 막는다. 이 오류는 **모든 파일을 다 읽은
뒤 마지막 단계에서** 터져 작업 전체를 날리기 때문에, 이중 방어가 비용보다
이득이 크다.

**결정 23 — 문서키에 편 이름을 넣는다.**
실데이터 12개가 전부 문서번호 `NHNIS-BC-DS03`, 버전 `0.7` 로 같았다.
결과적으로 모든 파일이 `#2` ~ `#12` 로 구분되어 키가 아무 의미도 갖지
못했고, 중복 경고가 12번 울렸다. 실제 구분자는 표지 문서명 뒤쪽의 편
이름(`04.여신실적(1)`)이다. 공백으로 둘러싸인 대시를 기준으로 잘라
`NHNIS-BC-DS03-04.여신실적(1)-v0.7` 형태로 만든다.

대시 양쪽 공백을 요구하는 이유는 `NHNIS-BC-DS03` 자체가 하이픈을 포함하기
때문이다. `#N` 보정은 그대로 남겨 두되, 이제는 진짜 중복일 때만 울린다.

**결정 24 — 미분류 영역은 시트에 묻지 말고 실행 로그로 알린다.**
실데이터 12개에서 `기타` 가 130건 나왔다. 엑셀을 열어 필터를 걸어야만
원인을 알 수 있으면 대응이 늦어지므로, 실행이 끝날 때 분류하지 못한 영역
이름을 건수와 함께 출력한다. 그 이름을 `AREA_KIND` 에 넣으면 끝나는
문제라, 이름만 알면 고칠 수 있게 만드는 것이 핵심이다.

**결정 25 — `화면별버튼` 은 계산하지 않고 `UI목록` 을 투영한다.**
필요한 값(`버튼`, `버튼수`)이 이미 `UI목록` 에 있으므로 컬럼만 골라 낸다.
같은 값을 두 번 계산하면 두 시트가 어긋날 여지가 생긴다.

**한글 변형 여부 검증 (2026-09-19).**
추출 결과에서 `삭제` 가 `식제` 로 보인다는 보고가 있었다. 보유한 3개
파일의 요소 3,721건을 검사한 결과 조합형(NFD) 자모 혼입 0건, NFC 정규화
시 변하는 문자열 0건이었다. `clean_text` 는 제어문자만 제거하고
`split_items` 는 공백만 정리하므로 한글 음절을 바꿀 경로가 없다.
원본 확인이 필요하다.

**결정 26 — `화면별버튼` 을 콤마 목록에서 매트릭스로 바꿨다.**
한 셀에 `조회, 엑셀, 인쇄, …` 로 몰아넣으면 화면 하나는 읽히지만
화면끼리 비교가 안 된다. "이 화면엔 있는데 저 화면엔 없네" 를 보려면
열을 맞춰야 한다. 그래서 행 = 화면, 열 = 버튼, 값 = ● 인 매트릭스로
바꿨다. 콤마 목록은 `UI목록` 의 `버튼` 컬럼에 그대로 남아 있어
정보가 사라지지는 않는다.

**결정 27 — 버튼 컬럼을 사용 화면수 내림차순으로 둔다.**
가장 많이 쓰는 버튼이 왼쪽에 모이므로 왼쪽 덩어리가 곧 공통 버튼
세트가 된다. 그 덩어리에 생긴 구멍이 "공통 버튼이 빠진 화면" 으로
바로 눈에 들어온다. 알파벳순이나 등장순으로 두면 이 효과가 사라진다.

**결정 28 — 헤더를 세로로 세운다.**
버튼이 37종이면 가로 헤더로는 컬럼을 좁힐 수 없어 한 화면에 다 안
들어온다. 헤더를 90도 회전시키면 컬럼 폭을 4.2 로 줄이고도 이름이
다 읽힌다. 표시 셀에는 배경색을 넣어 덩어리와 구멍이 도형처럼 보이게 했다.

**결정 29 — 버튼 이름이 고정 컬럼명과 겹치면 뒤에 `_` 를 붙인다.**
엑셀 표(ListObject)는 헤더가 중복되면 파일이 깨진다. 설계서에
`버튼수` 라는 이름의 버튼이 생길 가능성은 낮지만, 깨지는 쪽의
대가가 커서 막아 둔다.

**결정 30 — `등록`·`입력` 영역은 `입력` 이라는 별도 구분으로 둔다.**
실데이터에서 `등록 영역`(46건), `입력 영역`(10건)이 나왔다. 조회조건에
합칠까 했지만 성격이 다르다. 조회조건은 **걸러 보는** 값이고 등록·입력은
**저장할 값을 넣는** 곳이다. 둘을 섞으면 "등록 화면이 어디인가" 를 못
가려내므로 구분을 나누고 `UI목록` 에 `입력` / `입력수` 컬럼을 더했다.

규칙 순서상 `조건`·`조회` 를 `등록`·`입력` 보다 앞에 둔다. `조회조건 입력
영역` 같은 이름이 오면 조회조건으로 잡는 쪽이 맞기 때문이다.

**결정 31 — 코드·카테고리 영역은 데이터로 본다.**
`그룹코드 영역`, `상세코드 영역`, `분류상세코드 영역`, `그룹상세코드`,
`분류코드 영역`, `카테고리 영역` 이 나왔다. 모두 코드관리 화면의 목록
그리드라 데이터 영역과 같은 성격이다. `그룹상세코드` 처럼 `영역` 이
안 붙은 이름도 있어, 키워드는 `코드` / `카테고리` 로 잡는다.

**결정 32 — 콜론 없는 줄은 앞 영역의 연속으로 본다.**
`(미분류)` 가 55건으로 가장 많았다. 원인은 작성자가 Shift+Enter 대신
Enter 를 눌러 한 영역의 항목이 문단째로 나뉜 경우로 추정된다. 이전에는
이런 줄을 새 영역으로 세어 이름 없는 `(미분류)` 가 쌓였다. 이제는 앞
영역의 항목으로 이어 붙인다. 앞에 영역이 하나도 없을 때만 `(미분류)` 로
남긴다.

되돌릴 수 있는 선택이다. 잘못 붙어도 요소는 사라지지 않고 옆 영역에
들어갈 뿐이며, `화면요소` 시트에서 눈으로 확인할 수 있다.

**결정 33 — 분류 규칙 순서를 "보여 주는 영역 먼저" 로 재정렬했다.**
`화면리스트 영역` 을 넣다가 잠재 버그를 발견했다. 기존 순서는
`조건` → `조회` → … → `결과` 였는데, `조회결과 영역` 같은 이름이 오면
`조회` 에 먼저 걸려 **조회조건으로 잘못 분류**된다. 이런 이름은 미분류로
빠지지도 않아서 로그에도 안 나타나고 조용히 틀린다.

그래서 순서를 바꿨다. 결과·리스트·목록·코드·카테고리·데이터처럼
**보여 주는 영역**을 먼저 보고, 그 다음에 조건·조회, 마지막에 등록·입력을
본다. 기존 13종 전부 같은 결과가 나오는지 회귀 검증했다.

규칙이 키워드 기반인 이상 순서가 곧 우선순위이므로, 규칙을 추가할 때는
**기존 이름이 그대로 분류되는지 확인**하는 절차가 필요하다.

**결정 34 — 실행 로그 첫 줄에 스크립트 버전을 찍는다.**
결과가 앞 실행과 똑같이 나온 일이 있었다. 원인은 GitHub raw 의 CDN
캐시(최대 5분)로 이전 버전 파일이 내려온 것이었는데, 알아채는 데 왕복
한 번을 썼다. 로그 첫 줄에 `ui_spec_extract 2026-09-19e` 를 찍으면
어느 버전이 돌았는지 바로 보인다. 내려받을 때는 브랜치 주소 대신
커밋 SHA 를 박은 주소를 쓴다.

**결정 35 — `(1/2)(2/2)` 로 나뉜 슬라이드를 한 화면으로 합친다.**
`중복점검` 에 걸린 41종이 사실은 ID 오류가 아니라, **한 화면을 두 장에
나눠 그린 것**이었다. "슬라이드 1장 = UI 1개" 라는 처음 가정이 틀렸다는
뜻이라 병합으로 바로잡았다.

합치는 대상은 **쪽 표기가 있고 UI_ID 와 이름이 같은 것뿐**이다. 표기 없는
화면이 휩쓸려 합쳐질 여지를 없애기 위해서다.

병합 효과가 생각보다 컸다. 검증해 보니 둘째 장(2/2)은 화면 캡처만 있고
표에는 `기타 제약조건` 한 줄만 들어 있는 경우가 많았다. 병합 전에는 이
슬라이드가 `조회조건`·`버튼`·`데이터` 가 전부 빈 행으로 남아 있었는데,
앞서 "UI목록에 값이 있는 셀과 없는 셀이 섞여 있다" 는 지적의 원인 중
하나가 이것이었다.

**결정 36 — 합치되 슬라이드 단위 추적은 남긴다.**
`UI목록` 은 논리적 화면 단위로 한 행이지만, `슬라이드_전체` 컬럼에
`4, 5` 처럼 출처를 남긴다. `UI흐름_상세` 와 `화면요소` 의 각 행은 원래
슬라이드 번호를 그대로 유지하므로, 어떤 항목이 몇 번째 장에서 왔는지
언제든 되짚을 수 있다.

**결정 37 — 이어 붙일 때 같은 내용은 한 번만 남긴다.**
두 장에 같은 흐름 문구가 복사돼 있으면 합쳤을 때 그대로 두 번 기록된다.
`(UI_KEY, 섹션, 내용)` 과 `(UI_KEY, 영역, 요소명)` 기준으로 중복을 걸러낸
뒤 순번을 다시 매긴다. 실데이터 3개 파일에서는 두 장의 내용이 서로 달라
걸러진 행이 없었지만(1,277행·3,721행 그대로), 문서마다 사정이 다르므로
방어해 둔다.

**결정 38 — 쪽 표기와 실제 장수가 다르면 경고한다.**
`(1/2)` 인데 짝이 되는 장이 없으면 설계서에서 슬라이드가 누락됐다는
뜻이다. 조용히 한 장짜리로 처리하지 않고 경고를 띄운다.

## 알려진 한계
- `UI 레이아웃` 영역의 화면 캡처 이미지와 ①②③ 말풍선 번호는 추출하지 않는다.
  (이미지 자체가 데이터가 아니라서. 필요하면 이미지 파일로 따로 뽑는 기능 추가 가능)
- 보고서 유형 화면은 `버튼 영역` 자체가 없어 `버튼` 컬럼이 비는 것이 정상이다.
- 영역 요소 분해는 `화면 정의` 섹션만 대상이다. `업무처리 흐름` 은 문장이라
  콤마로 쪼개는 것이 의미가 없다.
- 버튼 인벤토리는 표기가 다르면 다른 버튼으로 센다. 예) `엑셀` 과
  `엑셀 다운로드` 는 별개. 동의어 통합은 넣지 않았다(문서 원문을 왜곡하지
  않기 위함).
- 표지에는 라벨 셀이 없어 도형 이름 위치로만 읽는다. 대신 버전과 작성일의
  형식을 검사해 이상하면 경고한다.
- 표지 문서명 표기가 문서마다 일정하지 않다. 샘플에서 `04.여신실적(2)` 가
  아니라 `여신실적(2)` 로, `02.종합실적` 파일이 `01.종합실적` 으로 적혀
  있었다. 문서키는 표지 원문을 그대로 따르므로 원본 오기는 그대로 드러난다.
  고치지 않는 쪽이 맞다고 보았다.
- 샘플의 슬라이드 4·5·7은 제목이 모두 `2.1.1 종합현황` 으로 동일하다.
  원본 문서의 복사-붙여넣기 흔적으로 보이며, 추출기는 있는 그대로 옮긴다.
